interface:
  display_name: "Session Sync"
  short_description: "Save/read scoped Codex Markdown memory."
  default_prompt: "Use $session-sync to save this session to the memory-session root, or to work/home scope when I say so."
